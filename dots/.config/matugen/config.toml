[config]
version_check = false

# NOTE: hamr, hyprland(colors.lua), hyprlock, gtk3, fuzzel and discord were moved
# out of matugen and into scripts/colors/applycolor.sh, so they track BOTH wallpaper
# and custom themes (matugen never runs for custom themes). gtk4 stays here because
# applycolor writes a separate colors.css. waybar templates were removed (waybar
# is not in use; reverted to the ii shell).

[templates.m3colors]
input_path = '~/.config/matugen/templates/colors.json'
output_path = '~/.local/state/quickshell/user/generated/colors.json'

[templates.gtk4]
input_path = '~/.config/matugen/templates/gtk-4.0/gtk.css'
output_path = '~/.config/gtk-4.0/gtk.css'

[templates.kde_colors]
input_path = '~/.config/matugen/templates/kde/color.txt'
output_path = '~/.local/state/quickshell/user/generated/color.txt'

[templates.wallpaper]
input_path = '~/.config/matugen/templates/wallpaper.txt'
output_path = '~/.local/state/quickshell/user/generated/wallpaper/path.txt'
