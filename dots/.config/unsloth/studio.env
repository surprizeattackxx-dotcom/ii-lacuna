# Sourced by systemd user unit unsloth-studio.service (optional lines only).
#
# Gated / HF Hub models need a token:
#   huggingface-cli login
# then either rely on ~/.cache/huggingface/token or set:
# HF_TOKEN=hf_xxxxxxxx
#
# If downloads use a proxy:
# HTTPS_PROXY=http://127.0.0.1:8888
# HTTP_PROXY=http://127.0.0.1:8888
# NO_PROXY=localhost,127.0.0.1
