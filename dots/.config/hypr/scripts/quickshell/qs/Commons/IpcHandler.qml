import QtQuick

// Stub: Noctalia plugins declare IPC handlers we don't need to execute.
// Provides the same property surface so QML doesn't error on instantiation.
QtObject {
    property string target: ""
}
