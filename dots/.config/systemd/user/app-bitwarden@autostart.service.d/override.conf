[Unit]
After=noctalia.service

[Service]
# Electron registers its StatusNotifierItem once and never retries, so it must
# not start before noctalia owns the watcher name or the tray icon is lost.
ExecStartPre=/usr/bin/gdbus wait --session --timeout 30 org.kde.StatusNotifierWatcher
